## What's new

- **Lab 12 — QPACK** — first-principles Encoded Field Section decode (RFC 9204), static / `RIC=0`
- **`lab qpack`** — `--demo chrome|firefox|safari`, fixtures `qpack_chrome` / `qpack_firefox` / `qpack_safari`
- **H3 HEADERS** — `h3_chrome` now carries real QPACK with Chromium pseudo order `m,a,s,p`
- Explicit reject of dynamic-table (`RIC>0`) so REs learn the encoder-stream boundary
- Teaching lock: QPACK `:method GET` = index **17** (HPACK uses **2**)

## Try it

```bash
coherencelab lab qpack --demo chrome
coherencelab lab qpack --fixture qpack_firefox
coherencelab lab h3 --fixture h3_chrome
```

See [`docs/labs/12-qpack.md`](https://github.com/oliver-nyx/coherencelab/blob/main/docs/labs/12-qpack.md).

Windows lab curriculum (Labs 01–12) is complete. Live Safari ClientHello remains deferred (macOS/iOS).
